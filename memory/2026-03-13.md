Initialized `BbangMxn.github.io` as a Quartz-based wiki/blog.
Chose an Obsidian-style authoring workflow with content stored in `content/`.
Added a GitHub Pages Actions deploy workflow targeting the `main` branch.
